# TIER 0 ✅ (crash/corruption risk)

Nothing left! handleQuit was fixed.
# TIER 1 (mandatory subject requirements — non-negotiable)

[] KICK #channel nick [:reason] — not started. Subject IV.1 requires this.
[] INVITE nick #channel — not started. Subject IV.1 requires this. (Also: Channel has no invite-list concept — +i invite-only channels can't be joined yet, INVITE would fix that.)
[] Root README.md at repo root — Subject Ch. V requires: italic first line with logins, Description/Instructions/Resources sections, English. Doesn't exist yet.
[x] Stop tracking ircserv/ircserv binary — it's in git; needs .gitignore + git rm --cached.
[x] irssi live-verify 

# TIER 2 (defense-checklist housekeeping)

[] Verify "only allowed external functions used" (subject p.6) — fcntl only as F_SETFL, O_NONBLOCK
[] Verify "exactly one poll()" — read and check
#  TIER 3 (real bugs, lower severity — protocol correctness, no crash)

[] handlePart malformed target — passes "" instead of replyTarget(client) in 3 error replies
[] JOIN missing self-echo — client should see their own :nick JOIN #channel line
# TIER 4 (cosmetic, zero impact)

 Dead AddMemberResult enum in Channel.hpp — decide: wire through or delete
 File mismatch: topicHandler.cpp actually contains handleMode (and handleTopic too)
# TIER 5 (optional, low priority)

 Case-insensitive commands (pass as well as PASS)
 Multi-channel JOIN #general,#food — you flagged this as skippable